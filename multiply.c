int mlpy() 																				/*Function for multiplication*/
{
	int a,b;
	printf(" Enter two numbers:"); 
	scanf ("%d %d", &a,&b); 															/*Asking the user for two inputs*/
	printf(" %d x %d = %d\n",a ,b, a*b);												/*Prin
}
